from .Plato import *
from .Persona import *

class Familia (object):
    nombref = None
    personas = []



    def __init__(self):
        self.personas = []

    def set_nombref(self,nf):
        self.nombref=(nf)

    def agregar_persona(self,p):
        self.personas.append(Persona)

    def promed_familiar(self,nf):
        if nombref==nf:
            #
            return sum(self.cal_totales) / len(self.personas)

    def mas_gordo(self, nf):
        if nombre==nf:
            for item in self.personas
                #
                return max()


    def menos_gordo(self, nf):
        if nombre==nf:
            for item in self.personas:
                #
                return min ()
